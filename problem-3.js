/* ৩. setTimeout এবং setInterval কিভাবে কাজ করে। এই দুইটার মধ্যে ডিফারেন্স কি */
// difference of set time out and setInterval
/*setTimeout function running one time . 
 and set setInterval function running infinite until it stopped. */
